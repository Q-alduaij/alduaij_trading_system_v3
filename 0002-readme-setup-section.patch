*** Begin Patch
*** Update File: README.md
@@
+## Setup & Health Check
+
+1. **Install deps**
+```bash
+python -m venv .venv
+source .venv/bin/activate  # Windows: .venv\Scripts\activate
+pip install -r requirements.txt
+```
+
+2. **Environment**
+```bash
+cp .env.example .env
+# Fill in your API keys (OpenRouter/OpenAI, Finnhub, AlphaVantage, NewsAPI, TwelveData, Polygon)
+# and MT5 credentials/path if you plan to use MetaTrader5 live/paper execution.
+```
+
+3. **Bootstrap local DB**
+```bash
+python utils/db_init.py
+```
+
+4. **API key health check**
+```bash
+python tools/api_key_health_check.py
+```
+The script validates keys and basic MT5 initialization. It exits non-zero on failures.
+
+5. **Configs**
+Edit `config/instruments.yaml` and `config/strategies.yaml` as needed. Keep `PAPER_TRADING=true` while testing.
+
*** End Patch
